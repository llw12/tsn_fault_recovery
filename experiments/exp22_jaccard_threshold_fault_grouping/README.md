# exp22: Jaccard-threshold fault grouping

Run `./run.sh --qualification` for algorithm qualification, `./run.sh --quick`
for M_RING at `1`, `3/5` and positive overlap, or `./run.sh --formal` for the
six-scenario preregistered threshold sweep.  This experiment does not invoke
OMNeT++ or INET.
