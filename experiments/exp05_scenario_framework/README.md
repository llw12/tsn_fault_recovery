# exp05: Scenario-Driven Experiment Framework

Run `./run.sh` to validate both YAML scenarios, compile deterministic OMNeT++ artifacts, execute Diamond and mesh10 in NoRecovery and Online modes, and regenerate `results/scenario_framework`.

The selected mesh10 fault is `l_sw2_sw5`: TT1, TT2, and TT7 are affected; seven TT flows are unaffected. Offline modes remain explicitly `NOT_IMPLEMENTED`.
